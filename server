const http = require('http');

const serverConn = http.createServer((req,res) => {
    console.log(req);
});
 
const PORT = 3000;
serverConn.listen(PORT, () => {
    console.log(`Port ${3000}`);
});

